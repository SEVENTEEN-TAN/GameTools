<Window x:Class="GameTools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameTools"
        mc:Ignorable="d"
        Title="游戏准星工具" Height="200" Width="200"
        AllowsTransparency="True" WindowStyle="None" 
        Background="Transparent" Topmost="True"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Loaded="Window_Loaded"
        WindowStartupLocation="Manual">
    <Grid>
        <!-- 准星 -->
        <Canvas x:Name="CrosshairCanvas" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Path x:Name="Crosshair" Stroke="#99FF0000" StrokeThickness="2" 
                  HorizontalAlignment="Center" VerticalAlignment="Center">
                <Path.Data>
                    <GeometryGroup>
                        <LineGeometry StartPoint="-10,0" EndPoint="10,0" />
                        <LineGeometry StartPoint="0,-10" EndPoint="0,10" />
                        <EllipseGeometry Center="0,0" RadiusX="3" RadiusY="3" />
                    </GeometryGroup>
                </Path.Data>
            </Path>
        </Canvas>
    </Grid>
</Window>
